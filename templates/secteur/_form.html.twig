
<div class="row">

    <div class="card">

        <card-body>

            {{ form_start(form) }}




            <button class="btn">{{ button_label|default('Save') }}</button>
            {{ form_end(form) }}


        </card-body>
    </div>
</div>

