{% extends 'base.html.twig' %}

{% block title %}ModelDocument index{% endblock %}

{% block body %}
    

         <div class="row">
         <div class="col-lg-12">
            <div class="card">
                <div class="card-header"><i class="fa fa-table"></i> Model Document </div>
                <div class="card-body">
            <div class=" mb-2 d-flex justify-content-center">
                <a class=" btn btn-outline-primary" href="{{path('add')}}"> Ajouter Document </a>
            </div>
        <div class=" mb-2 d-flex justify-content-center">
                <a class=" btn btn-outline-primary" href="{{path('model_document_make')}}"> Utiliser Document </a>
            </div>

 </div>
     </div>
    </div>

{% endblock %}
