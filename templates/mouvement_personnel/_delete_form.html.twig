<form method="post" action="{{ path('mouvement_personnel_delete', {'id': mouvement_personnel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mouvement_personnel.id) }}">
    <button class="btn">Delete</button>
</form>
