{% extends 'base.html.twig' %}

{% block title %}Edit MouvementPersonnel{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-12">
            <div class="card">

                <div class="card-body">
                    {{ include('mouvement_personnel/_form.html.twig', {'button_label': 'Update'}) }}

                    <a href="{{ path('mouvement_personnel_index') }}">back to list</a>

                    {{ include('mouvement_personnel/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}
