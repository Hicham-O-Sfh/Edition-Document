{% extends 'base.html.twig' %}

{% block title %}Edit MotifRejet{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header" id="headingOne">
        <h5 class="mb-0 text-center">
            <button class="btn btn-link">
                Motif Départ
            </button>
        </h5>
    </div>

    <div id="collapseOne" class="collapse show" aria-labelledby="headingOne" data-parent="#accordion">
        <div class="card-body">
            <div>
                <div class="d-flex mb-2">
                    <div class="mr-auto p-2">
                        <a class=" btn btn-outline-primary  " href="{{ path('parametrage_spe') }}"><svg width="1em"
                                height="1em" viewBox="0 0 16 16" class="bi bi-arrow-return-left" fill="currentColor"
                                xmlns="http://www.w3.org/2000/svg">
                                <path fill-rule="evenodd"
                                    d="M5.854 5.646a.5.5 0 0 1 0 .708L3.207 9l2.647 2.646a.5.5 0 0 1-.708.708l-3-3a.5.5 0 0 1 0-.708l3-3a.5.5 0 0 1 .708 0z" />
                                <path fill-rule="evenodd"
                                    d="M13.5 2.5a.5.5 0 0 1 .5.5v4a2.5 2.5 0 0 1-2.5 2.5H3a.5.5 0 0 1 0-1h8.5A1.5 1.5 0 0 0 13 7V3a.5.5 0 0 1 .5-.5z" />
                            </svg></a>
                    </div>
                    <div class="d-flex mb-2">
                        <div class="ml-auto p-2">
                            {{ include('motif_rejet/_delete_form.html.twig') }}
                        </div>
                    </div>
                </div>
            </div>
            <div class="w-100 text-center">
                {{ include('motif_rejet/_form.html.twig', {'button_label': 'Modifier'}) }}
            </div>
        </div>
    </div>

</div>
{# <h1>Edit MotifRejet</h1>

    {{ include('motif_rejet/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('motif_rejet_index') }}">back to list</a>

    {{ include('motif_rejet/_delete_form.html.twig') }} #}
{% endblock %}